# Laravel REST API kök adresi.
#
# TANIMLI DEĞİLSE: lib/api.ts mock veriye düşer (lib/mockData.ts).
# TANIMLIYSA:      lib/api.ts gerçek HTTP isteği atar.
#
# Backend hazır olunca .env.local dosyası oluşturup burayı doldur.
# .env.local ASLA commit edilmez (bkz. .gitignore).
#
# NEXT_PUBLIC_API_URL=https://api.hasanavdunyasi.com/api/v1
